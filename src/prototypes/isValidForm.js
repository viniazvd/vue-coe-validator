// export default function (form = defaultForm(this.validations, defaultFormName)) {
//   const isValid = Object.entries(this.validations[form]).every(([key, { isValid }]) => isValid)

//   return isValid
// }
